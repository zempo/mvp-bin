import React, { useReducer } from "react";

const WorksState = (props) => {
  const initialState = {
    works: [],
    searchWorks: [],
  };

  return (
    <div>
      <h1></h1>
    </div>
  );
};

export default WorksState;
